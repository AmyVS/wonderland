<%= form_for(@line) do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, :placeholder => "New line name..."%>
  </div>
  <%= f.submit("Enter",:class => "btn") %>
<% end %>
